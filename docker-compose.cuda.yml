services:
  inference_ray:
    command: serve run inference_ray/deploy.cuda.yml
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
    gpus:
      - driver: nvidia
        count: 1
        capabilities: [gpu]
