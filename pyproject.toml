[project]
name = "TIB-AV-A"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "packaging>=24.2",
    "pyyaml>=6.0.2",
    "requests>=2.32.3",
    "tqdm>=4.67.1",
]

[tool.uv.workspace]
members = [
    "packages/tibava_data",
    "packages/tibava_interface",
    "packages/tibava_utils",
    "analyser",
    "backend",
    "inference_ray",
]
